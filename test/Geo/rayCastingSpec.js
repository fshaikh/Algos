/**
 * Tests for ray casting algorithm for point in polygon problem
 */

const expect = require('chai');

describe('#rayCasting',()=>{
    describe('simple convex polygon',()=>{
        it('should return true when point is inside the polygon',()=>{

        });
        it('should return false when point is outside the polygon',()=>{

        });
        it('should return true when point is at the boundary of the polygon',()=>{

        });
        it('should return false when point passes through a vertex of the polygon',()=>{

        });
    });

    describe('simple concave polygon',()=>{
        it('should return true when point is inside the polygon',()=>{

        });
        it('should return false when point is outside the polygon',()=>{

        });
        it('should return true when point is at the boundary of the polygon',()=>{

        });
        it('should return false when point passes through a vertex of the polygon',()=>{

        });
    });
});